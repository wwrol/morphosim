# morphosim
A simulation tool for reaction-diffusion systems.

This project uses uv. To install uv, run pip install uv. To build the project, run uv sync.
To run a simulation, install uv and build the project. Then, run one of the following simulations:
* uv run main_exponential.py
* uv run main_interactions.py
